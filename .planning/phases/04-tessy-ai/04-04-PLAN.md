---
phase: 04-tessy-ai
plan: 04
type: execute
wave: 4
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
  - 04-03-PLAN.md
files_modified:
  - scripts/sync/test-superproject-sync.ps1
  - scripts/sync/superproject-sync.ps1
  - scripts/sync/superproject-sync-status.ps1
  - scripts/sync/import-module-changes.ps1
  - SYNC.md
  - README.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Verification for this phase must prove operator safety, not just script existence."
    - "Dry-run support is mandatory for both outbound and inbound sync paths."
    - "The final runbook should let a future operator execute the workflow without reopening the discuss context."
  artifacts:
    - path: "scripts/sync/test-superproject-sync.ps1"
      provides: "Deterministic smoke and regression checks for the sync workflow"
      min_lines: 120
    - path: "SYNC.md"
      provides: "Final verification checklist and troubleshooting guide"
      min_lines: 180
    - path: "README.md"
      provides: "Public-facing summary of the finalized sync capability"
      min_lines: 100
  key_links:
    - from: "scripts/sync/test-superproject-sync.ps1"
      to: "scripts/sync/superproject-sync.ps1"
      via: "exercise outbound dry-run and safety checks"
      pattern: "DryRun|Module|Block"
    - from: "scripts/sync/test-superproject-sync.ps1"
      to: "scripts/sync/import-module-changes.ps1"
      via: "exercise inbound dry-run and conflict gating"
      pattern: "DryRun|Import|Conflict"
---

<objective>
Close the phase with verification tooling, dry-run regression checks, and final operator documentation so the
sync workflow is usable and trustworthy after implementation lands.

Purpose: convert the earlier waves into a verified operational system rather than a loose collection of scripts.
Output: a repeatable smoke test surface plus final runbooks and troubleshooting guidance.
</objective>

<execution_context>
@E:/tessy-argenta-fenix/README.md
@E:/tessy-argenta-fenix/SYNC.md
@E:/tessy-argenta-fenix/.planning/phases/04-tessy-ai/04-CONTEXT.md
@E:/tessy-argenta-fenix/.planning/phases/04-tessy-ai/04-RESEARCH.md
@E:/tessy-argenta-fenix/.planning/phases/04-tessy-ai/04-01-PLAN.md
@E:/tessy-argenta-fenix/.planning/phases/04-tessy-ai/04-02-PLAN.md
@E:/tessy-argenta-fenix/.planning/phases/04-tessy-ai/04-03-PLAN.md
@E:/tessy-argenta-fenix/scripts/sync/superproject-sync.ps1
@E:/tessy-argenta-fenix/scripts/sync/superproject-sync-status.ps1
@E:/tessy-argenta-fenix/scripts/sync/import-module-changes.ps1
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Add a deterministic sync smoke test harness</name>
  <files>scripts/sync/test-superproject-sync.ps1</files>
  <read_first>scripts/sync/superproject-sync.ps1, scripts/sync/superproject-sync-status.ps1, scripts/sync/import-module-changes.ps1</read_first>
  <action>Create a test harness that exercises the status command, outbound dry-run, inbound dry-run, and representative blocked-state scenarios. Keep it deterministic and local-first; prefer fixture or simulated repo-state checks over fragile live pushes where possible.</action>
  <verify>Run the test harness end to end and confirm it exits non-zero when a required command or safety gate regresses.</verify>
  <acceptance_criteria>
    - The harness covers outbound, inbound, and blocked-state behavior.
    - Failures are readable and point to the broken command path.
    - The harness is safe to run repeatedly on a developer machine.
  </acceptance_criteria>
  <done>The phase has a repeatable smoke harness that proves the sync workflow still behaves safely.</done>
</task>

<task type="auto">
  <name>Task 2: Harden script flags and dry-run ergonomics</name>
  <files>scripts/sync/superproject-sync.ps1, scripts/sync/superproject-sync-status.ps1, scripts/sync/import-module-changes.ps1</files>
  <read_first>scripts/sync/test-superproject-sync.ps1, SYNC.md</read_first>
  <action>Normalize script flags, exit codes, and console output so the sync workflow feels like one cohesive toolset. Ensure each script supports enough dry-run/preview behavior for the runbooks and smoke tests to stay honest.</action>
  <verify>Run each script with `-Help` or equivalent and with dry-run/preview flags; confirm the interfaces line up with the docs and the test harness.</verify>
  <acceptance_criteria>
    - Core scripts share a consistent argument and exit-code style.
    - Dry-run mode exists wherever mutations would otherwise happen.
    - Output is suitable for both humans and future automation wrappers.
  </acceptance_criteria>
  <done>The sync toolset exposes consistent flags, previews, and exit semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize troubleshooting and operator docs</name>
  <files>SYNC.md, README.md</files>
  <read_first>SYNC.md, README.md, scripts/sync/test-superproject-sync.ps1</read_first>
  <action>Finalize the docs with a verification checklist, known failure modes, bypass guidance, recovery paths, and a concise public summary of what the superproject sync capability does. Keep the README short and the runbook detailed.</action>
  <verify>Perform a cold-read of the docs and confirm a future operator could install, inspect, dry-run, sync, and recover without reopening Phase 4 planning artifacts.</verify>
  <acceptance_criteria>
    - Troubleshooting covers missing remotes, dirty repos, divergence, and partial sync.
    - README stays concise while `SYNC.md` carries operational detail.
    - The final docs match the actual script names and flags exactly.
  </acceptance_criteria>
  <done>Future operators can run and recover the sync workflow from the shipped docs alone.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| Verification harness -> sync scripts | Tests validate operator-facing behavior and safety gates | flags, exit codes, blocked-state expectations |
| Dry-run output -> operator trust | Preview mode influences whether users trust mutation commands | target modules, paths, actions, block reasons |
| Final docs -> long-term maintenance | Runbooks become the durable interface for future operators and agents | install steps, smoke steps, recovery guidance |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-A | False confidence | smoke harness | mitigate | Cover blocked states and regression-sensitive dry-run flows, not only happy paths. |
| T-04-04-B | Unsafe mutation | script ergonomics | mitigate | Require preview/dry-run support and consistent exit codes before considering the phase complete. |
| T-04-04-C | Documentation drift | README/SYNC | mitigate | Validate docs against actual script names, flags, and troubleshooting scenarios before closeout. |
</threat_model>

<verification>
Before declaring wave 4 complete:
- [ ] `pwsh -File scripts/sync/test-superproject-sync.ps1`
- [ ] Dry-run/preview check for each sync script
- [ ] Cold-read of `README.md` and `SYNC.md` against the implemented command surface
- [ ] Manual checklist confirming install, status, outbound dry-run, inbound dry-run, and blocked-state handling
</verification>

<success_criteria>
- The sync workflow has a repeatable regression gate.
- Preview and safety ergonomics are strong enough for real operator use.
- Final docs are sufficient for future execution without reopening planning context.
</success_criteria>
