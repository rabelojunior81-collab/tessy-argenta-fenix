---
phase: 04-tessy-ai
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
files_modified:
  - scripts/sync/superproject-sync-status.ps1
  - scripts/sync/import-module-changes.ps1
  - scripts/sync/superproject-sync.ps1
  - SYNC.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Inbound sync from module repos back to the root is manual by design."
    - "Conflict detection must block the import path before root files are mutated."
    - "Status visibility is a first-class operator feature, not just debug output."
  artifacts:
    - path: "scripts/sync/superproject-sync-status.ps1"
      provides: "Readable root/module status report across configured repos"
      min_lines: 120
    - path: "scripts/sync/import-module-changes.ps1"
      provides: "Manual inbound sync tool with divergence and conflict checks"
      min_lines: 160
    - path: "SYNC.md"
      provides: "Operator runbook for manual reconciliation and conflict recovery"
      min_lines: 150
  key_links:
    - from: "scripts/sync/import-module-changes.ps1"
      to: "scripts/sync/sync.config.json"
      via: "resolve which module can be imported and where it lands in root"
      pattern: "modules|path"
    - from: "scripts/sync/superproject-sync-status.ps1"
      to: "scripts/sync/import-module-changes.ps1"
      via: "status output tells the operator when import is safe"
      pattern: "ahead|behind|dirty|conflict"
---

<objective>
Implement the explicit module -> root reconciliation path and the status tooling that makes the hybrid sync model
operable without guesswork.

Purpose: give the operator a manual, conflict-aware import workflow instead of silently assuming root is always
the only source of truth.
Output: status reporting plus a guarded import tool that can bring module-side changes back into the superproject.
</objective>

<execution_context>
@E:/tessy-argenta-fenix/AGENTS.md
@E:/tessy-argenta-fenix/SYNC.md
@E:/tessy-argenta-fenix/.planning/phases/04-tessy-ai/04-CONTEXT.md
@E:/tessy-argenta-fenix/.planning/phases/04-tessy-ai/04-RESEARCH.md
@E:/tessy-argenta-fenix/.planning/phases/04-tessy-ai/04-02-PLAN.md
@E:/tessy-argenta-fenix/scripts/sync/sync.config.json
@E:/tessy-argenta-fenix/scripts/sync/superproject-sync.ps1
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Build a multi-repo sync status report</name>
  <files>scripts/sync/superproject-sync-status.ps1</files>
  <read_first>scripts/sync/sync.config.json, AGENTS.md, .planning/phases/04-tessy-ai/04-RESEARCH.md</read_first>
  <action>Create a status command that inspects the root and each configured module repository, reporting dirty state, ahead/behind indicators, remote availability, and whether outbound or inbound sync is currently safe. Keep the output concise enough for terminal use but explicit enough to drive operator decisions.</action>
  <verify>Run the status command in a clean workspace and in a prepared dirty/divergent scenario; confirm the report distinguishes safe, blocked, and action-needed states.</verify>
  <acceptance_criteria>
    - Status output covers root plus all configured modules.
    - Dirty and divergent states are called out clearly.
    - The report tells the operator which command to run next.
  </acceptance_criteria>
  <done>The operator can inspect root and module sync readiness from one status surface.</done>
</task>

<task type="auto">
  <name>Task 2: Implement manual module import into the root workspace</name>
  <files>scripts/sync/import-module-changes.ps1, scripts/sync/superproject-sync-status.ps1</files>
  <read_first>scripts/sync/import-module-changes.ps1, scripts/sync/sync.config.json, .planning/phases/04-tessy-ai/04-CONTEXT.md</read_first>
  <action>Implement a manual import command that targets one configured module at a time, validates that the root and the module are in a safe state, fetches or inspects module changes as needed, and copies/reconciles the module content back into the root path only when the operator explicitly invokes it. Fail with a clear block reason if there is any conflict or ambiguous overlap.</action>
  <verify>Run the import tool in dry-run mode first, then against a controlled module-side change, and confirm it refuses unsafe states and prints the exact files or directories it would reconcile.</verify>
  <acceptance_criteria>
    - Imports are explicit and module-scoped.
    - Unsafe root/module state blocks the command before mutation.
    - The operator can preview what will be imported before doing it.
  </acceptance_criteria>
  <done>Module-side changes can be brought back into root only through an explicit guarded import flow.</done>
</task>

<task type="auto">
  <name>Task 3: Document the manual pull workflow and recovery path</name>
  <files>SYNC.md</files>
  <read_first>SYNC.md, .planning/phases/04-tessy-ai/04-CONTEXT.md, .planning/phases/04-tessy-ai/04-RESEARCH.md</read_first>
  <action>Extend `SYNC.md` with a concrete operator runbook for reading status, importing module changes, resolving blocked states, and recovering when a replication run partially succeeds. Keep the documentation honest about what is automated versus what still needs deliberate human action.</action>
  <verify>Follow the runbook once in a dry walkthrough and confirm every command it names actually exists and matches the script flags/output.</verify>
  <acceptance_criteria>
    - The inbound sync path is understandable without reading the scripts.
    - Recovery steps exist for blocked or partial sync scenarios.
    - The docs never imply that conflicts will be merged automatically.
  </acceptance_criteria>
  <done>The manual reconciliation runbook explains import, recovery, and blocked-state handling clearly.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| Module repo state -> status tool | Nested repo state determines operator decisions | dirty flags, ahead/behind, remote health |
| Operator command -> root workspace | Manual import may copy module changes back into root | selected module, changed files, target paths |
| Runbook -> human action | Docs shape whether operators bypass or respect the safety model | import sequence, conflict handling, recovery steps |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-A | Confused deputy | import tool | mitigate | Require explicit module selection and preview before any root mutation. |
| T-04-03-B | Tampering | root reconciliation | mitigate | Refuse imports when root or module state is dirty/divergent in unsafe ways. |
| T-04-03-C | Operator error | status/runbook | mitigate | Make status output prescriptive and keep the runbook aligned with actual script flags. |
</threat_model>

<verification>
Before declaring wave 3 complete:
- [ ] `pwsh -File scripts/sync/superproject-sync-status.ps1`
- [ ] `pwsh -File scripts/sync/import-module-changes.ps1 -Module <name> -DryRun`
- [ ] Controlled dirty/divergent scenario check to confirm the import path blocks cleanly
- [ ] Runbook walkthrough in `SYNC.md` against the actual script interface
</verification>

<success_criteria>
- The manual inbound sync path is explicit, safe, and inspectable.
- Operators can tell whether sync is safe before mutating root or module repos.
- Conflict policy remains manual and fail-closed.
</success_criteria>
